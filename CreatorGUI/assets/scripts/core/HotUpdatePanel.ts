import { _decorator } from 'cc';
import BasePanel from './base/BasePanel';

const { ccclass } = _decorator;

@ccclass('HotUpdatePanel')
export class HotUpdatePanel extends BasePanel {
    start() {

    }

    update(deltaTime: number) {

    }
}


